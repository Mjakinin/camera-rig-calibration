"""Atomic publication services behind the stable :mod:`publication` facade.

The modules separate immutable dataset publication, method-result adaptation,
inventory/report generation, and transaction finalization.
"""
