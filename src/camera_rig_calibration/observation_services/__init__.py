"""Observation detection, quality filtering, selection, and freezing services.

The stable public selection API remains available from
``camera_rig_calibration.observations``.  These modules contain the focused
implementation steps used by runtime, preflight, and the wizard.
"""
