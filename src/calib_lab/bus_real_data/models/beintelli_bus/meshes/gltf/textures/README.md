# glTF Textures

Texture images used by the BeIntelli bus glTF model.

These files are visual assets. They affect rendering appearance, not calibration logic. Keep filenames stable because they are referenced by `scene.gltf`.
