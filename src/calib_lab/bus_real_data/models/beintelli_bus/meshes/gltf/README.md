# glTF Mesh Assets

This folder contains the glTF representation of the BeIntelli bus model.

Typical content:

```text
scene.gltf
scene.bin
textures/
```

The `.gltf` file references binary geometry data and texture images. Keep the relative paths between these files unchanged unless you also update the glTF scene file.
