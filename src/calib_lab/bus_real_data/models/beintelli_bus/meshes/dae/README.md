# COLLADA Mesh Assets

This folder contains the COLLADA `.dae` representation of the BeIntelli bus model.

The file is used as one possible mesh representation for Gazebo/model tooling. Keep it together with the other mesh variants and avoid editing it manually unless the model export pipeline changes.
