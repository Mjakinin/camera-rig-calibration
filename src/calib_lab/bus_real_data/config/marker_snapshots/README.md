# Marker Snapshots

`marker_snapshots` stores saved marker-placement snapshots for reproducibility and debugging.

Use this folder when you need to keep a known-good marker layout while experimenting with new marker placements or world-generation changes.

## Rule

Snapshots document configuration states. They are not calibration results. Final calibration outputs belong in:

```text
results/bus_real_data/
```
