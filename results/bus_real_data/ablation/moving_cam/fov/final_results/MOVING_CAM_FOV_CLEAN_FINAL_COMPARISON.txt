MOVING-CAMERA FOV ABLATION — CLEAN SOURCE STATUS
==================================================

Scope:
Camera-to-GT comparison status for moving-camera FOV ablation.
This is not a complete AP01/AP02/AP03 comparison yet because only fov_40deg has partial method outputs.

Final source policy:
- AP01 must use the same final camera-map SE(3) evaluation as RES before it can be reported as final.
- AP02 must use the official GT-aligned full-map SE(3) evaluator and must contain all four static cameras.
- AP03 must use final Multi-ArUco output only; intermediate files and copied files are rejected.
- Missing or invalid methods are listed explicitly as EXCLUDED / FAILED / NOT_RUN.

Final status table:

variant   | method | status        | mean_t_cm | mean_r_deg | cam0_t | cam0_r | cam1_t | cam1_r | cam3_t | cam3_r | cam5_t | cam5_r | note
----------+--------+---------------+-----------+------------+--------+--------+--------+--------+--------+--------+--------+--------+-----------------------------------------------------------------------------------------------
fov_40deg | AP01   | NEEDS_REEVAL  | -         | -          | -      | -      | -      | -      | -      | -      | -      | -      | legacy AP01 output exists, but final RES-style camera-map SE(3) evaluation is missing
fov_40deg | AP02   | FAILED        | -         | -          | -      | -      | -      | -      | -      | -      | -      | -      | AP02 official evaluation aborted because cam_edge_5 estimated static-camera pose is missing
fov_40deg | AP03   | NOT_RUN       | -         | -          | -      | -      | -      | -      | -      | -      | -      | -      | no AP03 final Multi-ArUco output found

Observed diagnostic details:
- AP01 legacy run completed, but its output is not the final camera-map SE(3) evaluation used for RES.
- AP01 legacy FOV result is poor for cam_edge_0 and cam_edge_5, so it should not be silently treated as a clean final AP01 row.
- AP02 initialized/exported only 3 static cameras and failed during GT comparison because cam_edge_5 was missing.
- AP03 has no result files under the FOV ablation folder.

Conclusion:
FOV is structurally cleaned, but scientifically incomplete.
To make FOV comparable to RES, rerun/fix:
1. AP01 final camera-map SE(3) evaluator for fov_40deg.
2. AP02 so that all four static cameras are estimated, or keep AP02 as FAILED.
3. AP03 final Multi-ArUco for fov_40deg, or keep AP03 as NOT_RUN.

Reusable finalization workflow:
1. Run each AP method per ablation variant and freeze method outputs.
2. Build a source-gated row set.
3. Accept only final, variant-specific method outputs.
4. Reject copied hashes, intermediate outputs, and incomplete camera sets.
5. Keep invalid method/variant combinations as explicit status rows.
6. Write exactly one final TXT.
7. Clean the ablation folder so only final_results/<FINAL_TXT> remains.
