name: Repository payload guard

on:
  push:
  pull_request:

jobs:
  repository-payload:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate regular-Git payload policy
        run: python3 run/check_repository_payload.py
