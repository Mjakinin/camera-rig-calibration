# Shared Tools

Utilities that are not specific calibration methods.

```text
capture/      # dataset generation, route preview, image capture, detector helpers
live_sim/     # Gazebo/live marker pose utilities
migration/    # temporary migration helpers
```

Tools may support data acquisition or maintenance, but final calibration results should be written to `results/bus_real_data/`.
