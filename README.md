# Archive branch: beintelli_bus_model

This branch stores the BeIntelli bus model package separately from the main `bus_real_data` calibration work.

If the main `bus_real_data` world depends on bus model assets, keep the required assets or a documented dependency in `main`.
